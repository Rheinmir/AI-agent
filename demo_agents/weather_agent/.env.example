# Copy file này thành .env (đã .gitignore, không commit) và điền MỘT trong hai key dưới đây
# trước khi chạy live. DeepSeek dùng endpoint OpenAI-compatible (model "deepseek-chat") — xem
# demo_agents/weather_agent/model_provider.py.
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
